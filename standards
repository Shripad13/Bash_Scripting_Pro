# Non Functional Requirements (NFR)
1. Code should be DRY.
2. Credentials should not be hard coded. --->> because if you push ur code to github repo & someone can see & misuse
3. Re-run of code should not fail. --->> Ex - Creating file/Directory in script each time will fail script.
4. Code should be multi-environment compatible. --->> Dev/QA/Prod


## My Domain --->> devsecopswithshri.site


# DNS Records for the internal components should always be private :

1. Frontend: should be accessible from the internet, so will be creating the DNS recording using Public-IP-Address  & we refer this as Public-DNS-Record.

2. 


# Domain Migration Takes 24 hrs of time to reflect
1. We will buy a domain on Go-Daddy/ Hostinger
2. We will create a PUBLIC hosted zone on AWS (& copy the NS servers from this hosted zone)
3. Then on the NS section of hosted zone on the domain provider, update the NS with the above one.
4. Then going forward, all the requests that comes to the domain provider will be routed to AWS Route53. for domain transalation.

while true; do nslookup decsecopswithshri.site; sleep 5; done


# Flow of Request:

1. User sends a request to the public DNS record.
2. Request is go through ISP to reach AWS Route53.
3. The request is routed through the AWS Load Balancer (NLB).
4. The NLB forwards the request to the Kubernetes Ingress Controller.
5. The Ingress Controller routes the request to the appropriate service within the cluster.
6. The service forwards the request to the backend pods.


decsecopswithshri.site  ---> ISP ---> Route53 Public Hosted Zone ---> NLB ---> Ingress Controller ---> Service ---> Pods


## https Vs https

http --->> Port 80  --->> Non Secure --> communication will be in plain text
https --->> Port 443 --->> Secure  --> communication will be encrypted using SSL/TLS certificate
# SSL/TLS Certificate :
1. DigiCert, SSL.com, GlobalSign, Cloudflare , Let's Encrypt (Free) are the top SSL certificate providers.
2. Using AWS Certificate Manager (ACM) we can request & manage SSL/TLS certificates. & attache to LB.
3. Two Types of Certificates :
   1. Public Certificate --->> used for public websites
   2. Private Certificate --->> used for internal applications
4. Regular Certificate --->> used for single domain
5. Wildcard Certificate --->> used for multiple sub-domains (*.devsecopswithshri.site)
   

1. Get the certificate from ACM.
2. Create a CNAME record with the value provided by ACM in the DNS provider
3. Once the CNAME is verified by ACM, the certificate will be issued.
4. Attach the certificate to the Load Balancer.
5. That should work for both argo.devsecopswithshri.site & expense.argo.devsecopswithshri.site & vault.devsecopswithshri.site
6. We will deploy a ingress controller with one LB & will use that LB to route the traffic to different services based on the host header.


## Install helm chart for nginx ingress controller
helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
helm upgrade -i ngx-ingress ingress-nginx/ingress-nginx -f ingress.yaml
                (chart name)   (path in the repo)    (values of your choice)


# Only Manual Task is to update the CNAME record of the ingress COntroller LB on the Route 53 for expense & argocd application, which can also be automated using External DNS tool.


## How External DNS works?
Need to create an External DNS service account with IAM role having permissions to manage Route 53 records.
Reference- https://github.com/kubernetes-sigs/external-dns



## Tools - 
Linux 
Shell scripting
Ansible
GOCD
Terraform
Hashicorp Vault






> Standards to be followed :
1. Right now, whatever we create, we are going with open network (allow all)
2. In organization, we dont use default VPC
3. As per our network needs, we would need to create custom VPC with private & public subnets.
   
> What we have achieved is a basic version of the application.
1. We need to design a product keeping the security in to a consideration.
2. Only App users (Engineers & developers) should access the application through ILB.
3. Tools should never be open to public (Tools should be in private subnet/ default vpc)   
4. We need to create a bastion host to access the private instances.
5. Ensure we create the network & all the components keeping the numer of environments on to consideration. (Dev/QA/Prod)

> Points to keep in mind while designing the network :
1. Connect to VPN.
2. you will connect to server using SSO
3. Create an Infrastructure which is HIGH AVAILABLE & FAULT TOLERANT. 

> In organization, if we keep multiple accounts for multiple environments & projects:
1. If you have multiple accounts in AWS, its tough to manage them.
2. Dev Infra will be in dev-x aws account.
3. Prod Infra will be in prod-x aws account.
4. api Infra will be in api-x aws account.
5. Sandbox Infra will be in sandbox-x aws account.

> AWS Landing Zone - 
1. Will have a main account for a organization & inside that we will have multiple accounts tied.
2. In that main account, we will use a service called a AWS Landing Zone.
3. If you login to that main account, you will see all the accounts listed there.
4. You can switch between the accounts using AWS SSO.   

# Tages are very important -
1. BU
2. Sub BU
3. Org
4. Billing 
5. Environment
6. App
