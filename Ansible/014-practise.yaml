---
- name: complete webapp deployment
  hosts: all
  gather_facts: false
  become: true
  vars:
    packages:
      - git
      - curl
      - unzip
      - zip 
      - htop
    app_user: devops
    app_directory: /home/devops

  tasks:
    - name: Display hostname
      debug:
        msg: "Configuration server {{ inventory_hostname }}"
    - name: Display the OS
      debug:
        #msg: "OS is {{ ansible_distribution }}"   --- deprecated ---
        msg: "OS is {{ ansible_facts['distribution'] }}"
   # install packages via loops
    
    - name: install required packages
      apt:
        name: "{{ item }}"
        state: present
      loop: "{{ packages }}"

    # create application user
    - name: create application user
      user:
        name: "{{ app_user }}"
        shell: /bin/bash
        state: present

    # create multiple directory using loops
    - name: Create application directory
      file:
        path: "{{ app_directory }}/{{ item }}"
        state: directory
        owner: "{{ app_user }}"
        group: "{{ app_user }}"
        mode: '0755'
      loop:
        - mydirectory
        - configdir
        - inventorydir
    #copy file

    - name: copy application configuration
      copy:
        src: /home/devops/application.conf
        dest: "{{ app_directory }}/application.conf"
        owner: "{{ app_user }}"
        group: "{{ app_user }}"
        mode: '0644'

    - name: Install nginx
      apt:
        name: nginx
        state: present
      # when: ansible_os_family == "Debian"    -- deprecated ---
      when: ansible_facts['os_family'] == "Debian"        
      notify:
             - Restart nginx
  
  handlers:
    - name: Restart nginx
      service:
        name: nginx
        state: restarted
